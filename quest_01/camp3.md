# c1-camp3 (look)
---

Твоя цель — создать файл с именем `look`, который будет содержать одну команду, использующую `find`, для поиска элементов, соответствующих трём разным шаблонам.

---

## 1. Инструкции к заданию

Создай файл `look`, который будет искать и показывать в текущей директории и её подпапках:

1. Всё, что начинается с буквы `a` (или),

2. Все файлы, которые заканчиваются на букву `z` (или),

3. Все файлы, которые начинаются с `z` и заканчиваются на `a!`.

**Подсказка:** Используй `man find` (руководство по команде `find`).

### 🎯 Ожидаемый результат

Скрипт должен вывести список путей ко всем файлам и директориям, которые соответствуют хотя бы одному из трёх условий.

---

## 2. Подготовка (Get Ready)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем `look` (без расширения). | Здесь будет код твоего решения (одна команда). |
| **Сделать исполняемым** | `chmod +x look` | Чтобы скрипт можно было запустить через `./`. |

### 💻 Команды для создания файла:

```bash
touch look
```

## 3. Написание файла (Set) 💻

Вам нужно использовать команду `find`, которая ищет в текущей директории (`.`) и использует оператор `-o` (OR) для объединения различных критериев поиска. Для задания шаблонов используется опция `-name`.

### 🔎 Компоненты команды `find`:
1. Всё, что начинается с `a`:
* Шаблон: `a*`
* Команда: `-name "a*"`
2. Все файлы, которые заканчиваются на `z`:
* Шаблон: `*z`
* Команда: `-name "*z"`
3. Все файлы, которые начинаются с `z` и заканчиваются на `a!`:
* Шаблон: `z*a!`
* Команда: `-name "z*a!"`

### 💡 Объединение условий:
В `find` оператор `-o` (OR) объединяет условия. Вся команда должна быть заключена в круглые скобки `\( ... \)` для правильной группировки.
```bash
find . \( -name "a*" -o -name "*z" -o -name "z*a!" \)
```

### 📄 Содержимое файла `look`:
```bash
find . \( -name "a*" -o -name "*z" -o -name "z*a!" \)
```
**Важно:** Пробелы вокруг скобок и слэши `\(` и `\)` для экранирования скобок критически важны для синтаксиса `find`.

## 4. Проверка и Отправка (Upload) 📤

Запустите скрипт:

```bash
./look
```
Вывод должен быть списком путей, начинающихся с `a`, заканчивающихся на `z`, или соответствующих шаблону `z*a!`.

### 💾 Загрузка в репозиторий

Выполните стандартный цикл Git:

1. `git add look`
2. `git commit -m "c1-camp3: look complete"`
3. `git push`
   
После успешного `git push`, не забудьте нажать кнопку проверки на платформе.