# c1-camp1: MASTERtheLS (Освоение ls)
---

Твоя цель — создать Shell-скрипт **`MASTERtheLS`**, который будет содержать **одну командную строку** с командой `ls` и ее флагами. Эта команда должна выводить список **только директорий** из текущей папки, отсортированный по времени их **последнего доступа**, с игнорированием скрытых файлов.

## 1. Инструкции к заданию

"Now that you know who you are. You need to remember what you can do..."

В файле **`MASTERtheLS`** должна быть одна командная строка, которая выполняет следующие действия:

1. **Выводит список файлов и директорий** текущего каталога.
2. **Игнорирует скрытые файлы** (те, чьи имена начинаются с точки, например, `.git`).
3. **Сортирует результат** по времени **доступа** (`access time`), причем **самый новый** (недавно доступный) элемент должен быть **первым** (по возрастанию).
4. **Показывает только директории**, которые **заканчиваются** на символ слэша (`/`).
---
## 🎯 Ожидаемый результат
Скрипт должен вывести список, где первыми идут наименее недавно использованные директории, а последними — самые недавно использованные.

---

## 2. Get Ready (Подготовка)

### 🛠️ Необходимые действия

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем **`MASTERtheLS`** (без расширения). | Здесь будет код твоего решения. |
| **Сделать исполняемым** | `chmod +x MASTERtheLS` | Чтобы скрипт можно было запустить через `./`. |

### 💻 Команды для создания и настройки:

```bash
touch MASTERtheLS
chmod +x MASTERtheLS
```

## 3. Set (Написание скрипта) 💻

Скрипт будет содержать команду `ls` с комбинацией флагов.

* **Поиск флагов `ls`**:
    1. **Лист файлов и директорий, игнорируя скрытые**: По умолчанию команда `ls` уже игнорирует скрытые файлы.
    2. **Сортировка по времени доступа (старый-новый)**
    * `ls -t` сортирует по времени **модификации** (новые первыми)
    * `ls -u` использует время **доступа** вместо времени модификации.
    * `ls -r` инвертирует порядок сортировки (**старые первыми**).
    * Чтобы отсортировать по времени доступа (флаг `u`) и вывести старые первыми (по возрастанию, инвертируем флагом `r`), используем `-tur`.
    3. **Показывать только директории, заканчивающиеся на** `/`:
    * Флаг `-d` (directory) заставляет `ls` не перечислять содержимое директорий.
    * Флаг `-F` добавляет индикатор типа файла: слэш (`/`) для директорий, звездочку (`*`) для исполняемых файлов и т.д.

### 💡 Объединение команды:

Команда для получения всех файлов и директорий, отсортированных по времени доступа, с символом `/` в конце:

```bash
ls -utrF
```
Чтобы отфильтровать только директории, необходимо применить команду `grep` к выводу `ls -utrF`:
```bash
ls -utrF | grep '/$'
```
* `/` — ищет символ слэша.
* `$` — означает "конец строки" (т.е., ищем слэш в конце).

### 📄 Содержимое файла `MASTERtheLS`

Откройте файл `MASTERtheLS` и вставьте следующую **одну** командную строку:

```bash
ls -utrF | grep '/$'
```
## 4. Проверка и Отправка (Upload) 📤

Запустите скрипт:

```bash
./MASTERtheLS
```
Вывод должен быть списком только директорий из текущей папки, отсортированных по времени их последнего доступа (самый старый доступ сверху).

### 💾 Загрузка в репозиторий

Выполните стандартный цикл Git:

1. `git add MASTERtheLS`
2. `git commit -m "c1-camp1: MASTERtheLS complete"`
3. `git push`
   
После успешного `git push`, не забудьте нажать кнопку проверки на платформе.