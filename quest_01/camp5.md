# c1-camp5 (lookagain.sh)
---

Твоя цель — создать Shell-скрипт `lookagain.sh`, который будет искать все файлы, оканчивающиеся на `.sh`, а затем выводить только их имена (без `.sh`) в обратном алфавитном порядке (по убыванию).

---

## 1. Инструкции к заданию

Создай файл `lookagain.sh`, который будет:

1. Искать в текущей директории и её подпапках все файлы, заканчивающиеся на `.sh`.
2. Показывать только имя файла, без расширения `.sh`.
3. Сортировать полученные имена в порядке убывания (descending order), как показано в примере.
        *Подсказка: Может пригодиться команда `cut`.*

### 🎯 Ожидаемый результат

Пример использования: 
```bash
$ ./lookagain.sh | cat -e
file3$
file2$
file1$
$
```
Объяснение: Вывод должен содержать список файлов без расширения `.sh` (`file3`, `file2`, `file1`), отсортированных так, что `file3` идет первым (обратный алфавитный порядок). Символ `$` в конце каждой строки — это индикатор конца строки от команды `cat -e`.

---

## 2. Подготовка (Get Ready)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем `lookagain.sh`. | Здесь будет код твоего решения. |
| **Сделать исполняемым** | `chmod +x lookagain.sh` | Чтобы скрипт можно было запустить через `./`. |

### 💻 Команды для создания файла:

```bash
touch myfamily.sh
```
```bash
chmod +x lookagain.sh
```

## 3. Написание файла (Set) 💻

Эта задача требует конвейера из трех или четырех команд: `find` -> `cut` (или `sed`) -> `sort`.

### 🔎 Компоненты команды:
1. Поиск файлов `.sh`:
* `find . -name "*.sh"`
2. Удаление расширения `.sh`:
* Метод 1: `cut` (если имена не содержат точек)
    * Вывод `find` включает путь (например,` ./myfamily.sh`). Сначала нужно убрать путь.
    * `find . -name "*.sh" | cut -c 3-` (удаляет `./`).
    * Далее используем `sed` для удаления `.sh`: `sed 's/.sh$//'`
* Метод 2: `sed` (более надежный)
    * `find . -name "*.sh" | sed 's/\.sh$//'`
    * Чтобы удалить префикс пути `./` одновременно с расширением `.sh`:
```bash
find . -name "*.sh" | sed 's/^\.\///g; s/\.sh$//g'
```
3. Сортировка в порядке убывания:
    * `sort -r` (обратный порядок).

### 📄 Содержимое файла `myfamily.sh`:
Для корректной работы с переменной окружения `$HERO_ID` внутри скрипта, который вы будете запускать, используйте следующую конструкцию:
```bash
find . -name "*.sh" | sed 's/^\.\///; s/\.sh$//' | sort -r
```
**Разбор** sed 's/^\.\///; s/\.sh$//':
1. s/^\.\///: Найти начало строки (^), за которым следует ./, и заменить это на ничто. (Удаляет префикс пути).
2. s/\.sh$//: Найти .sh в конце строки ($) и заменить на ничто.

## 4. Проверка и Отправка (Upload) 📤

Создайте тестовые файлы:

```bash
touch file1.sh file2.sh file3.sh
```
Запустите скрипт:
```bash
./lookagain.sh
# file3
# file2
# file1
```

### 💾 Загрузка в репозиторий

Выполните стандартный цикл Git:

1. `git add lookagain.sh`
2. `git commit -m "c1-camp5: lookagain.sh complete"`
3. `git push`
   
*После успешного `git push`, не забудьте нажать кнопку проверки на платформе.*