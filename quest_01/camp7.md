# c1-camp7 (countfiles.sh)
---

Твоя цель — создать файл, чье имя является сложной строкой со специальными символами (`\?$, *'ChouMi'*$?\`), и поместить в этот файл только два символа: `01`.

---

## 1. Инструкции к заданию

Создай файл, который:
1. Имеет имя `\?$'ChouMi'*$?\` (включая все кавычки и специальные символы).
2. Содержит только текст `01` и ничего больше (никаких переводов строки).

### 🎯 Ожидаемый результат
1. Проверка наличия файла (команда `ls`):
```bash
$ ls | cat -e
"\?$*'ChouMi'*$?\"$
$
```
*(Показывает только этот файл и символ конца строки)*
2. Проверка содержимого (команда `cat`):
```bash
$ cat "\?$*'ChouMi'*$?\" | cat -e
01$
$
```
*(Показывает `01` и символ конца строки)*

---

## 2. Подготовка (Get Ready)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Скопировать точное имя файла: `"\?$*'ChouMi'*$?\"`. | Не ошибиться с кавычками и слэшами. |

## 3. Написание файла (Set) 💻

Для создания файла с таким сложным именем, которое содержит пробелы, кавычки, и спецсимволы, необходимо использовать сильные кавычки (одинарные `''`) или экранирование (обратный слэш `\`) для защиты имени от интерпретации Shell.

Имя файла: `"\?$*'ChouMi'*$?\"`

### 🔎 Стратегия:
1. Создание имени: Используем `echo` для вывода содержимого `01`.
2. Экранирование имени: Полностью заключаем целевое имя в одинарные кавычки (`'...'`) при перенаправлении вывода (`>`). Это заставляет Shell рассматривать всю строку как буквальное имя файла.
3. Содержимое: Выводим `01` с помощью `echo -n`, чтобы не добавлять символ перевода строки (`\n`). Если не использовать `-n`, файл будет содержать `01\n`, а нам нужно только `01`.


### 📄 Команда для создания файла:
Откройте файл `countfiles.sh` и вставьте следующую одну командную строку:
```bash
echo -n "01" > '"\?$*'\''ChouMi'\''*$?\"'
```
**Разбор имени:**
* Исходное имя: `"\?$*'ChouMi'*$?\"`
* Внешние кавычки (`"`): Экранируются, но внутри строки, заключенной в одинарные кавычки, они не интерпретируются.
* Одинарные кавычки (`'`): Чтобы включить `'` в строку, заключенную в `' '`, мы должны завершить строку (первая `'`), написать `\''` (экранированный апостроф), и снова открыть строку (последняя `'`).

## 4. Проверка и Отправка (Upload) 📤

Проверьте, что в файле нет перевода строки, используя `cat -e`:

```bash
cat '"\?$*'\''ChouMi'\''*$?\"' | cat -e
```
*Должен вывести 01 и символ конца строки (но не перевод строки после 01)*
Ожидаемый вывод:
01$

### 💾 Загрузка в репозиторий

Проверьте, что в файле нет перевода строки, используя cat -e:

1. `git add '"\?$*'\''ChouMi'\''*$?\"'`
2. `git commit -m "c1-camp7: special file complete"`
3. `git push`
   
*После успешного `git push`, не git commit -m "c1-camp7: special file complete"забудьте нажать кнопку проверки на платформе.*
