# c1-camp2 (Символ окончания строки)
---

Твоя цель — создать файл с именем `r`, который содержит только символ `R` и символ перевода строки (`\n`).

---

## 1. Инструкции к заданию

"keep training..."

Инструкция: Создай файл `r`, который при выполнении команды `cat -e` выводит `R$`.

## 🎯 Ожидаемый результат
При запуске команды:
```bash
$ cat -e r
R$
$
```
### Объяснение:
* Символ `R` — это содержимое файла.
* Символ `$` — это индикатор конца строки (`\n`), который добавляет команда `cat -e`.
* Таким образом, файл `r` должен содержать только букву `R`, за которой следует перевод строки.
---

## 2. Подготовка (Get Ready)

### 🛠️ Необходимые действия

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем **`r`** (без расширения). | Файл, который мы будем наполнять. |

### 💻 Команды для создания файла:

```bash
touch r
```

## 3. Написание файла (Set) 💻

Чтобы поместить в файл `r` ровно один символ `R` и один символ перевода строки, удобнее всего использовать команду `echo` и перенаправление вывода (`>`).

### 📄 Содержимое файла `r` (Команда для заполнения)
```bash
echo "R" > r
```
Как это работает: Команда `echo "R"` выводит **"R"** и автоматически добавляет за ним символ перевода строки (`\n`). Оператор `>` перенаправляет этот вывод в файл `r`, перезаписывая его.

## 4. Проверка и Отправка (Upload) 📤

Проверка содержимого:

```bash
cat -e r
```
Вывод должен быть ровно: `R$`

### 💾 Загрузка в репозиторий

Когда файл `r` содержит нужные символы, его нужно загрузить в репозиторий.

1. `git add r`
2. `git commit -m "c1-camp2: r complete"`
3. `git push`
   
После успешного `git push`, не забудьте нажать кнопку проверки на платформе.