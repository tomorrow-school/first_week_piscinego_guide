# c1-camp6 (countfiles.sh)
---

Твоя цель — создать Shell-скрипт `countfiles.sh`, который будет выводить только общее количество обычных файлов и директорий, включая текущую директорию (`.`), внутри текущего каталога и всех его подкаталогов.

---

## 1. Инструкции к заданию

Создай файл `countfiles.sh`, который будет:

1. Считать общее количество обычных файлов (`regular files`) и директорий (`folders`).
2. Поиск должен вестись в текущей директории и всех её подпапках.
3. Текущая директория (`.`) должна быть включена в счет.
4. Выводить нужно только число (и ничего больше).

### 🎯 Ожидаемый результат

```bash
$ ./countfiles.sh | cat -e
12$
$
```
**Объяснение:** Вывод должен быть одним числом (`12` в примере), представляющим общее количество файлов и папок. Символ `$` в конце — индикатор конца строки от команды `cat -e`.

---

## 2. Подготовка (Get Ready)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем `countfiles.sh`. | Здесь будет код твоего решения. |
| **Сделать исполняемым** | `chmod +x countfiles.sh` | Чтобы скрипт можно было запустить через `./`. |

### 💻 Команды для создания файла:

```bash
touch myfamily.sh
```
```bash
chmod +x lookagain.sh
```

## 3. Написание файла (Set) 💻

Эта задача требует объединения двух команд в конвейере (`|`): `find` для поиска и `wc` для подсчета.

### 🔎 Компоненты команды:
1. Поиск файлов и папок: Используем `find .`.
* Обычные файлы: `find . -type f`
* Директории: `find . -type d`
* Объединение: Поскольку нам нужно посчитать И файлы, И директории, мы используем оператор `-o` (OR) для объединения условий, заключенных в скобки:
```bash
find . \( -type f -o -type d \)
```
   ***Важно: `find`** . по умолчанию включает в вывод текущую директорию (`.`), что соответствует требованию задания.*

2. Подсчет строк:
* Команда `wc -l` (word count - lines) подсчитывает количество строк, переданных ей по конвейеру. Каждая строка, выданная `find`, соответствует одному файлу или папке.

### 📄 Содержимое файла `countfiles.sh`:
Откройте файл `countfiles.sh` и вставьте следующую одну командную строку:
```bash
#!/bin/bash
find . \( -type f -o -type d \) | wc -l
```
   ***Важно:** Символы `\` и `(` `)` вокруг условий `-type f -o -type d` нужны для корректной группировки условий в `find`.*

## 4. Проверка и Отправка (Upload) 📤

Создайте несколько тестовых файлов и папок, чтобы проверить подсчет:

```bash
mkdir test_dir
touch test_file
```
Запустите скрипт:

```bash
./countfiles.sh
# Вывод должен быть одним числом (например, 4).
```

### 💾 Загрузка в репозиторий

Выполните стандартный цикл Git:

1. `git add countfiles.sh`
2. `git commit -m "c1-camp6: countfiles.sh complete"`
3. `git push`
   
*После успешного `git push`, не забудьте нажать кнопку проверки на платформе.*