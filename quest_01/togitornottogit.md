# 🎭 to-git-or-not-to-git... (Быть Git'у или не быть...)
---

Твоя цель — создать Shell-скрипт **`to-git-or-not-to-git.sh`**, который будет обращаться к файлу JSON, извлекать по ID **176** и выводить **три** поля: **имя**, **силу** и **пол** супергероя.

## 1. Инструкции к заданию

Напишите в файле `to-git-or-not-to-git.sh` команду, которая покажет **имя** (`name`), **силу** (`power`) и **пол** (`gender`) супергероя с **ID 176**.

* **Где искать:** Используй тот же источник данных с супергероями: `https://01.tomorrow-school.ai/assets/superhero/all.json`.
* **Что использовать:** `curl` и `jq`.

### 🎯 Ожидаемый результат (Пример вывода)

При запуске скрипта:
```bash
$ ./to-git-or-not-to-git.sh 
25
Female
```

## 2. Get Ready (Подготовка)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем **`to-git-or-not-to-git.sh`**. | Здесь будет код твоего решения. |
| **Сделать исполняемым** | `chmod +x to-git-or-not-to-git.sh` | Чтобы скрипт можно было запустить. |

### 💻 Команда для создания и настройки файла:

```bash
touch to-git-or-not-to-git.sh
chmod +x to-git-or-not-to-git.sh
```
## 3. Set (Написание скрипта) 💻

Скрипт должен использовать `curl` для получения JSON-данных и `jq` для их фильтрации.

### 📄 Содержимое файла `to-git-or-not-to-git.sh`

Используй конвейер (`|`) для передачи данных от `curl` к `jq` и далее к `tr` и `tail`.

```bash
#!/bin/bash

# URL для JSON-файла с данными
JSON_URL="[https://01.tomorrow-school.ai/assets/superhero/all.json](https://01.tomorrow-school.ai/assets/superhero/all.json)"

# 1. curl -s: Скачиваем JSON тихо
# 2. jq: Фильтруем по ID 176 и выводим name, power, gender
# 3. tr -d '"': Удаляем кавычки
# 4. tail -n +2: Пропускаем имя (первую строку) для соответствия примеру вывода

curl -s "$JSON_URL" | \
jq '.[] | select(.id == 176) | .name, .power | tostring, .gender' | \
tr -d '"' | \
tail -n +2
```

### 🧪 Проверка скрипта

1.  **Запустить скрипт:**
    ```bash
    ./to-git-or-not-to-git.sh
    ```
2.  **Ожидаемый вывод:**
    ```
    25
    Female
    ```
    > **Примечание:** Убедись, что файл исполняемый (`chmod +x to-git-or-not-to-git.sh`). Вывод должен содержать **только** эти две строки.

## 4. Upload (Загрузка в репозиторий) 📤

Когда скрипт работает и выводит ожидаемый результат, его нужно загрузить в твой репозиторий, используя команды Git.

### 💾 Команды и объяснение

Выполни эти команды **последовательно** в терминале, находясь в папке `piscinego`:

1.  Добавление файла:
**`git add to-git-or-not-to-git.sh`**

2. оздание коммита: 
**`git commit -m "здесь ты указываешь коментарий(пометку)твоего изменения\добавления"`**

3.  Отправка на сервер:
**`git push`**
   
## ✅ Финальный шаг
После успешного git push, вернись на платформу и запусти тест для проверки твоего решения.