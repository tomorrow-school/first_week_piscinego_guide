# c1-camp4 (myfamily.sh)
---

Твоя цель — создать Shell-скрипт `myfamily.sh`, который будет извлекать поле `relatives` (ключ: `relatives`) из JSON-файла супергероя, чей ID задан в переменной окружения `HERO_ID`.

---

## 1. Инструкции к заданию

Создай файл `myfamily.sh`, который будет показывать родственников (`relatives`) супергероя:

1. Поле для вывода: `relatives`.
2. Удалить кавычки из вывода.
3. ID супергероя берется из переменной окружения `HERO_ID`.
4. Источник данных: `https://01.tomorrow-school.ai/assets/superhero/all.json`.
5. Инструменты: `curl`, `jq` и другие.

### 🎯 Ожидаемый результат

Пример использования: 
```bash
$ export HERO_ID=1
$ ./myfamily.sh
Marlo Chandler-Jones (wife); Polly (aunt); Mrs. Chandler (mother-in-law); Keith Chandler, Ray Chandler, three unidentified others (brothers-in-law); unidentified father (deceased); Jackie Shorr (alleged mother; unconfirmed)
```

---

## 2. Подготовка (Get Ready)

| Действие | Что делать | Зачем |
| :--- | :--- | :--- |
| **Перейти в репозиторий** | Убедиться, что находишься в папке `piscinego`. | Все задания должны быть здесь. |
| **Создать файл скрипта** | Создать пустой файл с именем `myfamily.sh`. | Здесь будет код твоего решения. |
| **Сделать исполняемым** | `chmod +x myfamily.sh` | Чтобы скрипт можно было запустить через `./`. |

### 💻 Команды для создания файла:

```bash
touch myfamily.sh
```
```bash
chmod +x myfamily.sh
```

## 3. Написание файла (Set) 💻

Вам нужно использовать `curl` для получения данных, `jq` для парсинга и фильтрации по ID, а также для извлечения поля `relatives`.

### 🔎 Компоненты команды:
1. Получение данных: `curl -s [URL]`
2. Фильтрация по ID из переменной окружения: В `jq` можно использовать переменную Shell с помощью интерполяции в строке или, что безопаснее, передать переменную через опцию `--arg` или `--argjson`. Поскольку `HERO_ID` — это число, используем `$HERO_ID` внутри команды `find` или передадим его в `jq`.
Используем интерполяцию Shell (так как ID гарантированно будет числом):
```bash
jq ".[] | select(.id == $HERO_ID) | .connections.relatives"
```
   **Примечание**: Поле `relatives` находится внутри объекта `connections`.
3. Извлечение поля `relatives`: `.connections.relatives`
4. Удаление кавычек: `tr -d '"'`

### 📄 Содержимое файла `myfamily.sh`:
Для корректной работы с переменной окружения `$HERO_ID` внутри скрипта, который вы будете запускать, используйте следующую конструкцию:
```bash
#!/bin/bash
JSON_URL="https://01.tomorrow-school.ai/assets/superhero/all.json"
curl -s "$JSON_URL" | \
jq ".[] | select(.id == $HERO_ID) | .connections.relatives" | \
tr -d '"'
```

## 4. Проверка и Отправка (Upload) 📤

В терминале перед запуском скрипта необходимо установить переменную окружения:

```bash
export HERO_ID=1
./myfamily.sh
```
*Вывод должен соответствовать примеру (без кавычек).*

### 💾 Загрузка в репозиторий

Выполните стандартный цикл Git:

1. `git add myfamily.sh`
2. `git commit -m "c1-camp4: myfamily.sh complete"`
3. `git push`
   
*После успешного `git push`, не забудьте нажать кнопку проверки на платформе.*